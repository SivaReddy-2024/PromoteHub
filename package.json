{
  "name": "promotehub",
  "version": "1.0.0",
  "description": "PromoteHub - Promotional and Marketing Management Platform",
  "private": true,
  "scripts": {
    "build": "npm install --prefix server",
    "build:server": "npm install --prefix server",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "vercel-build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"blue,green\" \"npm run server\" \"npm run client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "marketing",
    "campaigns",
    "promotions",
    "express",
    "react",
    "mongodb"
  ],
  "author": "",
  "license": "ISC"
}
